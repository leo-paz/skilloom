---
"skilloom": patch
---

Show verified sessions using each skill instead of trace-file or repeated-read counts. Group retained evidence by native session, preserve installation and machine boundaries, and show recent sessions with last-use timestamps. Keep missing session identity explicit and group Codex subagents under verified root-session metadata without changing existing event identities or resetting backfill progress.
