---
"skilloom": patch
---

Read invocation declarations for installed skills with display names such as "Agent Browser" through their observed paths. Keep inferred paths restricted to safe directory names, and migrate older reader metadata without rejecting versioned snapshots.
